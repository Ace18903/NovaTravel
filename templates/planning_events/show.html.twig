{% extends 'base.html.twig' %}

{% block title %}PlanningEvents{% endblock %}

{% block body %}
    <h1>PlanningEvents</h1>

    <table class="table">
        <tbody>
        </tbody>
    </table>

    <a href="{{ path('app_planning_events_index') }}">back to list</a>

    <a href="{{ path('app_planning_events_edit', {'id_planning': planning_event.id_planning}) }}">edit</a>

    {{ include('planning_events/_delete_form.html.twig') }}
{% endblock %}
